{
  "name": "sddp-backend",
  "version": "0.1.0",
  "private": true,
  "description": "SDDP demo backend service — middle tier (frontend -> backend -> db).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20 <21"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
